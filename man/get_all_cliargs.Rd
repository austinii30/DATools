% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_args.R
\name{get_all_cliargs}
\alias{get_all_cliargs}
\title{Parse Multiple CLI Flags}
\usage{
get_all_cliargs(args, flags, valid_input_list)
}
\arguments{
\item{args}{Character vector of command-line arguments.}

\item{flags}{Character vector of flags to parse.}

\item{valid_input_list}{Named list. Each element contains the valid
inputs for the corresponding flag.}
}
\value{
A named list containing parsed and validated values for each flag.
}
\description{
Parse and validate values for multiple command-line flags. For each flag,
the corresponding values are extracted using \code{parse_cliarg()} and validated
using \code{validate_input()}.
}
\examples{
args <- c("--step", "clean", "train", "--model", "rf")

flags <- c("--step", "--model")

valid <- list(
  "--step" = c("clean", "train", "predict"),
  "--model" = c("rf", "svm")
)

get_all_cliargs(args, flags, valid)

}
\seealso{
Other cli_tools: 
\code{\link{parse_cliarg}()},
\code{\link{validate_input}()}
}
\concept{cli_tools}
